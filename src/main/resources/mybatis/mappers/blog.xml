<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">
	
	<!-- 쿼리문 작성 -->
	
	<insert id="insert" parameterType= "BlogVo">
		
		<![CDATA[
		
		insert into blog
		values(#{id}, #{blogTitle}, #{logoFile})
		
		]]>
		
	</insert>
	
	
	<update id="updateBasicInfo" parameterType="BlogVo">
	
		<![CDATA[	
			
			update 		blog
			set			blogTitle = #{blogTitle},
						logoFile = #{logoFile}
			where		id = #{id}
		]]>
	
	</update>
	
	
	<select id="selectInfo" parameterType="string" resultType="BlogVo">
		
		<![CDATA[
			
			select		userName,
						blogTitle,
						logoFile
			
			from 		blog b, users u
			where		id = #{id} and b.id = u.id
		]]>
	
	</select>
	

	
	<select id="selectBlog" parameterType="string" resultType="MainDto">
	
		<![CDATA[
		
	   select 	u.id,
       			userName,
       			blogTitle,
       			logoFile,
       			c.cateNo,
		        catename,
		        postNo,
		        postTitle,
		        postContent,    
		        p.regDate
	   
	   from users u, blog b, category c, post p
	   
	   where u.id = b.id and c.id = u.id
	   and u.id = #{id} and c.cateNo = (select max(cateNo) from category where id = #{id})
	   and postNo = (select max(postNo) from post p, category c 
	   where (select max(cateNo) from category where id = #{id}) = p.cateNo)
	   		
		
		]]>
		
	</select>

	

</mapper>
